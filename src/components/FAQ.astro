---
const faqItems = [
  {
    question: "What's the difference between Ralph Vibe and just using Claude Code?",
    answer:
      "Claude Code is powerful but has no memory between sessions. You restart, it forgets everything. Ralph Vibe gives Claude structure: specs to follow, plans to execute, one task at a time. It turns Claude Code into a reliable build system that can work autonomously while you're away.",
  },
  {
    question: "Can I use this on an existing project?",
    answer:
      "Yes. Run <code>ralph init</code> in any project folder. Ralph will interview you about your codebase, read your existing code, and create specs based on what's there. Then it can build what's missing.",
  },
  {
    question: "What if I want more control?",
    answer:
      "Don't use <code>--vibe</code>. Run each command separately: <code>ralph init</code>, then <code>ralph start</code>, then <code>ralph plan</code>, then <code>ralph work</code>. You control when each phase runs.",
  },
  {
    question: "What do I need to have installed?",
    answer:
      "Two things: Claude Code (the CLI) and a Claude Pro or Team subscription. Ralph Vibe uses Claude Code under the hood to do the actual building.",
  },
  {
    question: "Is it free?",
    answer:
      "Ralph Vibe is free and open source. You do need a Claude subscription ($20/month for Pro) since it uses Claude Code for the AI work.",
  },
  {
    question: "What happens when something goes wrong?",
    answer:
      "Each task runs independently. If a task fails, Ralph logs the error and moves on. You can review the logs, fix the issue, and re-run. Nothing half-finished gets committed — tests must pass.",
  },
];
---

<section id="faq" class="faq" aria-labelledby="faq-heading">
  <div class="container">
    <h2 id="faq-heading" class="section-heading" data-scramble-reveal="Frequently Asked Questions">Frequently Asked Questions</h2>

    <div class="faq-list">
      {
        faqItems.map((item) => (
          <details class="faq-item" data-faq-item>
            <summary class="faq-summary">
              <span class="faq-question" data-scramble>{item.question}</span>
              <span class="faq-icon" aria-hidden="true">
                <svg
                  width="20"
                  height="20"
                  viewBox="0 0 20 20"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <line x1="10" y1="4" x2="10" y2="16" class="vertical-line" />
                  <line x1="4" y1="10" x2="16" y2="10" />
                </svg>
              </span>
            </summary>
            <div class="faq-answer-wrapper">
              <div class="faq-answer" set:html={item.answer} />
            </div>
          </details>
        ))
      }
    </div>
  </div>
</section>

<script>
  // One-at-a-time behavior: close other details when one opens
  document.querySelectorAll('[data-faq-item]').forEach((details) => {
    details.addEventListener('toggle', () => {
      if ((details as HTMLDetailsElement).open) {
        document.querySelectorAll('[data-faq-item][open]').forEach((other) => {
          if (other !== details) {
            (other as HTMLDetailsElement).open = false;
          }
        });
      }
    });
  });
</script>

<style>
  .faq {
    padding: var(--space-8) var(--space-5);
    background: var(--void);
    position: relative;
    z-index: 10;
  }

  .container {
    max-width: 800px;
    margin: 0 auto;
  }

  .section-heading {
    font-family: var(--font-sans);
    font-size: clamp(1.75rem, 4vw, var(--text-section));
    font-weight: 600;
    text-align: center;
    margin: 0 0 var(--space-6);
    color: var(--text-primary);
    letter-spacing: -0.02em;
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .faq-item {
    border-bottom: 1px solid var(--concrete);
  }

  .faq-item:first-child {
    border-top: 1px solid var(--concrete);
  }

  .faq-summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
    padding: var(--space-4) 0;
    cursor: pointer;
    list-style: none;
  }

  .faq-summary::-webkit-details-marker {
    display: none;
  }

  .faq-summary::marker {
    display: none;
    content: '';
  }

  .faq-summary:focus-visible {
    outline: none;
    box-shadow: var(--shadow-focus);
    border-radius: 4px;
  }

  .faq-question {
    font-family: var(--font-sans);
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-primary);
    line-height: 1.4;
    text-align: left;
  }

  /* Monospace +/− icon in signal color */
  .faq-icon {
    flex-shrink: 0;
    color: var(--signal);
    transition: color var(--duration-state) var(--ease-out);
  }

  .faq-icon svg {
    display: block;
  }

  .faq-icon .vertical-line {
    transition: transform var(--duration-state) var(--ease-out),
                opacity var(--duration-state) var(--ease-out);
    transform-origin: center;
  }

  details[open] .faq-icon .vertical-line {
    transform: scaleY(0);
    opacity: 0;
  }

  details[open] .faq-icon {
    color: var(--signal-glow);
  }

  /* Animate height using CSS grid */
  .faq-answer-wrapper {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows var(--duration-layout) var(--ease-mechanical);
    overflow: hidden;
  }

  details[open] .faq-answer-wrapper {
    grid-template-rows: 1fr;
  }

  .faq-answer {
    min-height: 0;
    overflow: hidden;
  }

  .faq-answer {
    padding: 0 0 var(--space-4);
    font-family: var(--font-sans);
    font-size: 0.9375rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  /* Code elements in answers use signal color */
  .faq-answer :global(code) {
    font-family: var(--font-mono);
    font-size: 0.875em;
    background: var(--void-warm);
    padding: 0.125rem 0.5rem;
    border-radius: 4px;
    border: 1px solid var(--concrete);
    color: var(--signal);
  }

  /* Tablet and up */
  @media (min-width: 768px) {
    .faq-summary {
      padding: var(--space-4) 0;
    }

    .faq-question {
      font-size: 1.0625rem;
    }

    .faq-answer {
      padding: 0 0 var(--space-4);
    }
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .faq {
      padding: 80px var(--space-6);
    }

    .faq-question {
      font-size: 1.125rem;
    }
  }
</style>
