---
const features = [
  {
    title: "Fresh Start Every Time",
    description:
      "Each build iteration starts with zero memory. No accumulated confusion. No 'I thought I already did that.' Claude reads the specs fresh, picks a task, builds it, and exits.",
  },
  {
    title: "Specs Are the Truth",
    description:
      "The specs in your project are what gets built. Not vague ideas. Not old comments in code. Clear specifications that Claude follows exactly. When context resets, nothing is lost.",
  },
  {
    title: "No Homework Required",
    description:
      "No shell scripts to write. No prompt files to create. No architecture to understand. One command to start. The tool that simplifies Ralph was built with Ralph.",
  },
  {
    title: "Tests Keep It Honest",
    description:
      "One task at a time. Nothing is marked 'done' until tests pass. Built-in quality gates that prevent half-finished features from slipping through.",
  },
];

const comparison = [
  { pain: "Write shell scripts", solution: "Just run ralph work" },
  { pain: "Create prompt files manually", solution: "Auto-generated from interview" },
  { pain: "Understand the architecture", solution: "Just answer questions" },
  { pain: "Debug your setup", solution: "It just works" },
  { pain: "Hours of configuration", solution: "One command" },
];
---

<section id="features" class="features" aria-labelledby="features-heading">
  <div class="container">
    <h2 id="features-heading" class="section-heading" data-scramble-reveal="Why It Works">Why It Works</h2>

    <ul class="feature-grid" role="list">
      {features.map((feature) => (
        <li class="feature-card">
          <h3 class="feature-title" data-scramble>{feature.title}</h3>
          <p class="feature-description" data-scramble>{feature.description}</p>
        </li>
      ))}
    </ul>

    <div class="comparison-wrapper">
      <h3 class="comparison-heading" data-scramble-reveal="The Difference">The Difference</h3>
      <div class="comparison-table-container">
        <table class="comparison-table" aria-label="Comparison between Original Ralph Loop and Ralph Vibe">
          <thead>
            <tr>
              <th scope="col" class="comparison-header comparison-header-pain">
                <span class="header-bracket">[</span><span data-scramble>Original Ralph Loop</span><span class="header-bracket">]</span>
              </th>
              <th scope="col" class="comparison-header comparison-header-solution">
                <span class="header-bracket">[</span><span data-scramble>Ralph Vibe</span><span class="header-bracket">]</span>
              </th>
            </tr>
          </thead>
          <tbody>
            {comparison.map((row) => (
              <tr class="comparison-row">
                <td class="comparison-cell comparison-cell-pain" data-scramble>{row.pain}</td>
                <td class="comparison-cell comparison-cell-solution">
                  <code class="solution-code" data-scramble>{row.solution}</code>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>

<style>
  .features {
    padding: var(--space-8) var(--space-5);
    background: var(--void);
    position: relative;
    z-index: 10;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
  }

  .section-heading {
    font-family: var(--font-sans);
    font-size: clamp(1.75rem, 4vw, var(--text-section));
    font-weight: 600;
    text-align: center;
    margin: 0 0 var(--space-6);
    color: var(--text-primary);
    letter-spacing: -0.02em;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-4);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .feature-card {
    padding: var(--space-5);
    background: var(--void-warm);
    border: 1px solid var(--concrete);
    border-radius: 12px;
    position: relative;
    transition: transform var(--duration-state) var(--ease-smooth),
                border-color var(--duration-state) var(--ease-smooth);
  }

  .feature-card:hover {
    transform: translateY(-2px);
    border-color: var(--metal);
  }

  .feature-title {
    font-family: var(--font-sans);
    font-size: var(--text-card-title);
    font-weight: 600;
    margin: 0 0 var(--space-2);
    color: var(--text-primary);
    letter-spacing: -0.01em;
  }

  .feature-description {
    font-family: var(--font-sans);
    font-size: 0.9375rem;
    color: var(--text-secondary);
    margin: 0;
    line-height: 1.6;
  }

  /* Tablet: 2x2 grid */
  @media (min-width: 768px) {
    .feature-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-5);
    }

    .feature-card {
      padding: var(--space-5);
    }
  }

  /* Desktop: larger spacing and padding */
  @media (min-width: 1024px) {
    .features {
      padding: 80px var(--space-6);
    }

    .feature-grid {
      gap: 40px;
    }

    .feature-card {
      padding: var(--space-5);
    }

    .feature-title {
      font-size: 1.25rem;
    }
  }

  /* ==========================================================================
     Comparison Table - Industrial styling
     ========================================================================== */

  .comparison-wrapper {
    margin-top: var(--space-7);
  }

  .comparison-heading {
    font-family: var(--font-sans);
    font-size: clamp(1.25rem, 3vw, 1.5rem);
    font-weight: 600;
    text-align: center;
    margin: 0 0 var(--space-4);
    color: var(--text-primary);
    letter-spacing: -0.01em;
  }

  .comparison-table-container {
    background: var(--void-warm);
    border: 1px solid var(--concrete);
    border-radius: 12px;
    padding: var(--space-3);
    overflow-x: auto;
  }

  .comparison-table {
    width: 100%;
    border-collapse: collapse;
    font-family: var(--font-sans);
  }

  .comparison-header {
    font-family: var(--font-mono);
    font-size: var(--text-code);
    font-weight: 600;
    text-align: left;
    padding: var(--space-3);
    border-bottom: 1px solid var(--concrete);
    color: var(--text-primary);
    letter-spacing: var(--tracking-wide);
  }

  .header-bracket {
    color: var(--bracket);
  }

  .comparison-header-pain {
    width: 50%;
    background: rgba(255, 255, 255, 0.02);
  }

  .comparison-header-solution {
    width: 50%;
    background: rgba(212, 82, 42, 0.1);
  }

  .comparison-row {
    transition: background var(--duration-micro) var(--ease-out);
  }

  .comparison-row:hover {
    background: rgba(255, 255, 255, 0.02);
  }

  .comparison-cell {
    padding: var(--space-3);
    border-bottom: 1px solid var(--concrete);
    vertical-align: middle;
    color: var(--text-secondary);
    font-size: 0.9375rem;
  }

  .comparison-row:last-child .comparison-cell {
    border-bottom: none;
  }

  .comparison-cell-pain {
    color: var(--text-tertiary);
    text-decoration: line-through;
    text-decoration-color: var(--text-tertiary);
  }

  .comparison-cell-solution {
    font-weight: 500;
    color: var(--text-primary);
  }

  .solution-code {
    font-family: var(--font-mono);
    font-size: var(--text-code);
    background: rgba(212, 82, 42, 0.2);
    color: var(--signal-glow);
    padding: 0.25em 0.5em;
    border-radius: 4px;
    display: inline-block;
  }

  /* Mobile: stacked layout */
  @media (max-width: 599px) {
    .comparison-table-container {
      padding: var(--space-2);
    }

    .comparison-table,
    .comparison-table thead,
    .comparison-table tbody,
    .comparison-table tr,
    .comparison-table th,
    .comparison-table td {
      display: block;
    }

    .comparison-table thead {
      display: none;
    }

    .comparison-row {
      margin-bottom: var(--space-3);
      padding: var(--space-3);
      background: rgba(255, 255, 255, 0.02);
      border-radius: 8px;
      border: 1px solid var(--concrete);
    }

    .comparison-row:last-child {
      margin-bottom: 0;
    }

    .comparison-cell {
      padding: var(--space-2) 0;
      border-bottom: none;
    }

    .comparison-cell-pain {
      font-size: 0.875rem;
      margin-bottom: var(--space-1);
    }

    .comparison-cell-pain::before {
      content: '✗ ';
      color: var(--text-tertiary);
      text-decoration: none;
    }

    .comparison-cell-solution::before {
      content: '→ ';
      color: var(--signal);
    }
  }

  /* Tablet and up: side-by-side table */
  @media (min-width: 600px) {
    .comparison-table-container {
      padding: var(--space-4);
    }

    .comparison-header {
      padding: var(--space-4);
    }

    .comparison-cell {
      padding: var(--space-4);
    }
  }

  @media (min-width: 1024px) {
    .comparison-wrapper {
      margin-top: var(--space-8);
    }
  }
</style>
