---
const steps = [
  {
    number: "01",
    label: "Init",
    description: "Set up project files",
  },
  {
    number: "02",
    label: "Interview",
    description: "Describe what you want to build",
  },
  {
    number: "03",
    label: "Research",
    description: "Research APIs and approaches",
  },
  {
    number: "04",
    label: "Plan",
    description: "Create implementation tasks",
  },
  {
    number: "05",
    label: "Build",
    description: "Write code until done",
  },
];
---

<section id="how-it-works" class="how-it-works" aria-labelledby="how-it-works-heading">
  <div class="container">
    <h2 id="how-it-works-heading" class="section-heading">How It Works</h2>

    <ol class="steps" role="list">
      {steps.map((step, index) => (
        <>
          <li class="step">
            <span class="step-number" aria-hidden="true">[{step.number}]</span>
            <h3 class="step-label">{step.label}</h3>
            <p class="step-description">{step.description}</p>
          </li>
          {index < steps.length - 1 && (
            <li class="arrow" aria-hidden="true">
              <svg class="arrow-horizontal" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <path class="arrow-line" d="M5 12h14" />
                <path class="arrow-head" d="M12 5l7 7-7 7" />
              </svg>
              <svg class="arrow-vertical" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <path class="arrow-line" d="M12 5v14" />
                <path class="arrow-head" d="M5 12l7 7 7-7" />
              </svg>
            </li>
          )}
        </>
      ))}
    </ol>

    <p class="vibe-callout">
      With <code>--vibe</code>, it chains automatically. One command, zero babysitting.
    </p>
  </div>
</section>

<style>
  .how-it-works {
    padding: var(--space-7) var(--space-3);
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
  }

  .section-heading {
    font-family: var(--font-sans);
    font-size: clamp(1.5rem, 4vw, var(--text-section));
    font-weight: 600;
    text-align: center;
    margin: 0 0 var(--space-6);
    color: var(--text-primary);
    letter-spacing: -0.01em;
  }

  .steps {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-3);
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-5);
  }

  .step {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: var(--space-4);
    background: var(--void-warm);
    border-radius: 16px;
    border: 1px solid var(--concrete);
    width: 100%;
    max-width: 200px;
    box-shadow: var(--shadow-subtle);
    border-top: var(--edge-top);
    transition: transform var(--duration-micro) var(--ease-out),
                box-shadow var(--duration-micro) var(--ease-out);
  }

  .step:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-elevated);
  }

  .step-number {
    font-family: var(--font-mono);
    font-size: var(--text-bracket);
    font-weight: 500;
    color: var(--bracket);
    letter-spacing: 0.02em;
    margin-bottom: var(--space-2);
  }

  .step-label {
    font-family: var(--font-sans);
    font-size: 1.125rem;
    font-weight: 600;
    margin: 0 0 var(--space-2);
    color: var(--text-primary);
  }

  .step-description {
    font-family: var(--font-sans);
    font-size: 0.875rem;
    color: var(--text-secondary);
    margin: 0;
    line-height: 1.5;
  }

  .arrow {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .arrow-line {
    stroke: var(--concrete);
  }

  .arrow-head {
    stroke: var(--signal);
  }

  .arrow-horizontal {
    display: none;
  }

  .arrow-vertical {
    display: block;
  }

  .vibe-callout {
    text-align: center;
    font-family: var(--font-sans);
    font-size: 1rem;
    color: var(--text-secondary);
    margin: 0;
    padding: var(--space-4);
    background: var(--void-warm);
    border-radius: 12px;
    border: 1px solid var(--concrete);
    max-width: 500px;
    margin-left: auto;
    margin-right: auto;
    box-shadow: var(--shadow-subtle);
  }

  .vibe-callout code {
    font-family: var(--font-mono);
    background: var(--void);
    padding: 0.125rem 0.5rem;
    border-radius: 4px;
    font-size: 0.9em;
    color: var(--signal);
    border: 1px solid var(--concrete);
  }

  /* Tablet and up: horizontal flow */
  @media (min-width: 768px) {
    .how-it-works {
      padding: var(--space-8) var(--space-5);
    }

    .steps {
      flex-direction: row;
      justify-content: center;
      align-items: stretch;
      gap: var(--space-2);
    }

    .step {
      flex: 0 1 160px;
    }

    .arrow-horizontal {
      display: block;
    }

    .arrow-vertical {
      display: none;
    }
  }

  /* Desktop: slightly larger */
  @media (min-width: 1024px) {
    .steps {
      gap: var(--space-3);
    }

    .step {
      flex: 0 1 180px;
      padding: var(--space-4) var(--space-3);
    }
  }
</style>
