---
/**
 * Statement Section - Alche-style About section
 * Light background, left-aligned text with black highlight boxes
 */
---

<section id="statement" class="statement" data-theme="light" aria-labelledby="statement-heading">
  <div class="container">
    <h2 id="statement-heading" class="sr-only">Our Mission</h2>

    <div class="statement-content">
      <div class="statement-main">
        <p class="statement-line">
          <span class="highlight-box" data-scramble-reveal="Go for a walk.">Go for a walk.</span>
        </p>
        <p class="statement-line">
          <span class="highlight-box" data-scramble-reveal="Grab a coffee.">Grab a coffee.</span>
        </p>
        <p class="statement-line">
          <span class="highlight-box" data-scramble-reveal="Work on something else.">Work on something else.</span>
        </p>
      </div>

      <p class="statement-tagline" data-scramble-reveal="Your code keeps building.">
        Your code <em class="highlight">keeps building</em>.
      </p>
    </div>

    <div class="statement-meta" data-scramble-reveal="[ Autonomous Development ]">
      <span class="meta-bracket">[</span>
      <span class="meta-text">Autonomous Development</span>
      <span class="meta-bracket">]</span>
    </div>
  </div>

  <!-- Decorative geometric shape -->
  <div class="geometric-shape" aria-hidden="true">
    <svg viewBox="0 0 400 400" fill="none" stroke="currentColor" stroke-width="1">
      <polygon points="200,20 380,350 20,350" />
      <polygon points="200,60 340,310 60,310" />
    </svg>
  </div>
</section>

<style>
  .statement {
    padding: var(--space-8) var(--space-5);
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    z-index: 10;
    /* Light theme - transparent to show WebGL */
    background: transparent;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    width: 100%;
    padding-left: var(--space-4);
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .statement-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    margin-bottom: var(--space-6);
  }

  .statement-main {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .statement-line {
    margin: 0;
    line-height: 1;
  }

  /* Black highlight box behind text - Alche style */
  .highlight-box {
    display: inline;
    font-family: var(--font-sans);
    font-size: clamp(1.5rem, 4vw, 2.75rem);
    font-weight: 700;
    line-height: 1.4;
    letter-spacing: -0.02em;
    color: var(--output);
    background: var(--void);
    padding: 0.1em 0.3em;
    box-decoration-break: clone;
    -webkit-box-decoration-break: clone;
  }

  .statement-tagline {
    font-family: var(--font-sans);
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    font-weight: 400;
    color: var(--void);
    margin: 0;
    margin-top: var(--space-4);
    opacity: 0.7;
  }

  .statement-tagline .highlight {
    color: var(--signal);
    font-style: normal;
    font-weight: 600;
  }

  .statement-meta {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-mono);
    font-size: var(--text-bracket);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
  }

  .meta-bracket {
    color: var(--signal);
  }

  .meta-text {
    color: var(--void);
    opacity: 0.5;
  }

  /* Geometric shape - triangle like Alche's A */
  .geometric-shape {
    position: absolute;
    right: 10%;
    top: 50%;
    transform: translateY(-50%);
    width: clamp(250px, 30vw, 450px);
    height: clamp(250px, 30vw, 450px);
    color: var(--void);
    opacity: 0.08;
    pointer-events: none;
  }

  .geometric-shape svg {
    width: 100%;
    height: 100%;
  }

  @media (max-width: 768px) {
    .statement {
      padding: var(--space-7) var(--space-4);
    }

    .container {
      padding-left: 0;
    }

    .geometric-shape {
      right: -10%;
      opacity: 0.05;
    }
  }

  @media (min-width: 1024px) {
    .statement {
      padding: 160px var(--space-7);
    }

    .container {
      padding-left: var(--space-8);
    }

    .highlight-box {
      font-size: clamp(2rem, 4vw, 3.25rem);
    }

    .statement-tagline {
      font-size: 1.375rem;
    }
  }
</style>
