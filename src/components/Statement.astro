---
/**
 * Statement Section - Bold typographic mission statement
 * Inspired by Alche.studio's impactful text treatments
 * With sci-fi scramble text reveal animation
 */
---

<section id="statement" class="statement" aria-labelledby="statement-heading">
  <div class="container">
    <h2 id="statement-heading" class="sr-only">Our Mission</h2>

    <div class="statement-content">
      <p class="statement-text">
        <span class="statement-line" data-scramble-reveal="We believe coding should be">We believe coding should be</span>
        <span class="statement-line highlight-line" data-scramble-reveal="effortless."><em class="highlight">effortless</em>.</span>
      </p>

      <p class="statement-text">
        <span class="statement-line" data-scramble-reveal="Describe your vision.">Describe your vision.</span>
        <span class="statement-line" data-scramble-reveal="Let the machine build it.">Let the machine build it.</span>
        <span class="statement-line" data-scramble-reveal="Come back to working code.">Come back to <em class="highlight">working code</em>.</span>
      </p>
    </div>

    <div class="statement-meta" data-scramble-reveal="[ Autonomous Development ]">
      <span class="meta-bracket">[</span>
      <span class="meta-text">Autonomous Development</span>
      <span class="meta-bracket">]</span>
    </div>
  </div>
</section>

<style>
  .statement {
    padding: var(--space-7) var(--space-5);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--void);
    position: relative;
    z-index: 10;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    text-align: center;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .statement-content {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    margin-bottom: var(--space-7);
  }

  .statement-text {
    font-family: var(--font-sans);
    font-size: clamp(1.75rem, 5vw, 3.5rem);
    font-weight: 600;
    line-height: 1.2;
    letter-spacing: var(--tracking-tight);
    color: var(--text-primary);
    margin: 0;
  }

  .statement-line {
    display: block;
  }

  .highlight {
    color: var(--signal);
    font-style: normal;
  }

  .statement-meta {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    font-family: var(--font-mono);
    font-size: var(--text-bracket);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
  }

  .meta-bracket {
    color: var(--bracket);
  }

  .meta-text {
    color: var(--text-secondary);
  }

  @media (min-width: 768px) {
    .statement {
      padding: 160px var(--space-6);
    }
  }

  @media (min-width: 1024px) {
    .statement {
      padding: 200px var(--space-7);
    }

    .statement-text {
      font-size: clamp(2.5rem, 5vw, 4.5rem);
    }
  }
</style>
